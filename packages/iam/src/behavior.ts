/**
 * Placeholder for the future ResourcePolicy Behavior. The package is not yet
 * built; once it is, this file should declare a class with a
 * `/** @WebdaBehavior * /` JSDoc tag (compile-time discovery, no runtime
 * decorator) and implement the policy actions.
 */
export class ResourcePolicy {}
