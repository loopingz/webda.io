# Documentation

Always add JSDoc with description and examples

# Run tests

You have the test suite to run with `yarn test`, if you need to run a specific test run `yarn test -t ${myTest}` where myTest is the string of the test name you want to run.

# Typescript

We want to be able to run typescript in strict mode.
