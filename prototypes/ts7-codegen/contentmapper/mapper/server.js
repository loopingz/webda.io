// Entry point declared in package.json `typescript.contentMapper.exec`.
// The implementation lives in the prototype's own build so that it shares a
// single `typescript` install with the generators it reuses.
import "../../lib/mapper/server.js";
