# accessor vs Proxy

Webda leverage proxy, which allows you to centralize the business rules around your object.
It could have been implemented as getter/setter or accessor concept. But this would require to deduplicate all attributes and
alter the readibility.

Action First Arg => Body of PUT
Action Result => Return of PUT method
