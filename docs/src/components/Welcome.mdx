import Button from "@mui/material/Button";
import { WelcomeStatus } from "./WelcomeStatus";

## My Application

Inspect your running Webda application live — models, services, operations, requests, and logs — directly from this page.

<WelcomeStatus onTrySample={props.onClick} />
