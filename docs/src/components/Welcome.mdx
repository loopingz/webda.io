import Button from "@mui/material/Button";

## No application detected

You can visualize information from your Webda application by running the following command in its directory:

```shell
webda configuration
```

You will be able to access your models hierarchy, relations, and more.

:::info

If you just want to explore you can use our sample application.

<Button onClick={props.onClick}>Try sample application</Button>

:::
